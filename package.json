{
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean:ts": "tsc --build --clean",
    "clean:all": "rm -rf ./**/dist ./**/.next **/tsconfig.tsbuildinfo && npm run clean:ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@google/genai": "^2.0.1"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
